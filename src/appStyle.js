import { style } from 'typestyle';

export const rootStyle = style({
  fontFamily: 'Roboto',
});

export const newEntryElementStyle = style({
  position: 'fixed',
  bottom: 30,
  right: 30,
  fontSize: 30,
});
